#include <bits/stdc++.h>
using namespace std;

int safeMod(int x, int mod){
    int y = x % mod;
    if(y < 0)
        y += mod;
    return y;
}
/// Time complexity: O(1)

int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    ///  y = 2's mod inverse (mod m)


}
